# Docker Node 2 — uses container hostnames
node:
  id: 2
  grpc_port: 50052
  http_port: 8082

peers:
  - id: 1
    address: "node1"
    grpc_port: 50051
  - id: 3
    address: "node3"
    grpc_port: 50053

seats:
  rows: ["A", "B", "C"]
  cols_per_row: 4

heartbeat:
  interval_ms: 2000
  timeout_ms: 5000

election:
  timeout_ms: 3000
