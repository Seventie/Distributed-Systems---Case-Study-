# Docker Node 3 — uses container hostnames
node:
  id: 3
  grpc_port: 50053
  http_port: 8083

peers:
  - id: 1
    address: "node1"
    grpc_port: 50051
  - id: 2
    address: "node2"
    grpc_port: 50052

seats:
  rows: ["A", "B", "C"]
  cols_per_row: 4

heartbeat:
  interval_ms: 2000
  timeout_ms: 5000

election:
  timeout_ms: 3000
